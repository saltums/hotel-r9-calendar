name: Update Hotel Prices (disabled — use local run_and_push.ps1 instead)

# GitHub Actions は Cloudflare にブロックされるため無効化
# 代わりに run_and_push.ps1 をタスクスケジューラで実行してください
on:
  workflow_dispatch:  # 手動トリガーのみ残す（テスト用）

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Run run_and_push.ps1 locally instead."
